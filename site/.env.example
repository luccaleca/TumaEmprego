# Copie para site/.env e ajuste se necessário
# cp site/.env.example site/.env

DATABASE_URL="postgresql://tuma:tuma@localhost:5432/tuma_emprego?schema=public&connect_timeout=3"

# Opcional — adaptação completa do CV ao salvar busca (Cursor SDK)
# CURSOR_API_KEY=
