# Extensão — Chrome

Autofill em Gupy, LinkedIn, etc. Lê dados da API do `site/` (`localhost:3737`).

**Fase 3** do roadmap — depois de `site/` e `dados/` prontos.

```
extensao/
├── manifest.json
├── content.js      # roda na página da vaga
├── background.js
└── popup.html
```

Usuário revisa e clica Enviar — nunca envio automático.
